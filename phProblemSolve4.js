function password(obj){
    
        console.log(`${obj.siteName}#${obj.name}@${obj.birthYear}`)
    
}
const passObj ={
    name: "kolimudin",
    birthYear: 1999,
    siteName: "google"
}
password(passObj)